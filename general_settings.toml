#------------------------------------------------GENERAL SETTINGS-------------------------------------------------------
#    WALLETS_TO_WORK = 0 | Софт будет брать кошельки из таблице по правилам, описанным снизу
#    0       = все кошельки подряд
#    3       = только кошелек №3
#    [4, 20] = кошелек №4 и №20
#    [[5, 25]] = кошельки с №5 по №25
#    [[5, 25], [30, 35]] = кошельки с №5 по №25 и с №30 по №30

#    WALLETS_TO_EXCLUDE = 0 | Софт будет исключать кошельки из таблицы по правилам, описанным снизу
#    0       = никакой кошелек не будет убран
#    3       = только кошелек №3
#    [4, 20] = кошелек №4 и №20
#    [[5, 25]] = кошельки с №5 по №25
#    [[5, 25], [30, 35]] = кошельки с №5 по №25 и с №30 по №30

#    ACCOUNTS_IN_STREAM      | Количество кошельков в потоке на выполнение. Если всего 100 кошельков, а указать 10,
#                              то софт сделает 10 подходов по 10 кошельков

#    EXCEL_PASSWORD          | Включает запрос пароля при входе в софт. Сначала установите пароль в таблице
#    EXCEL_PAGE_NAME         | Название листа в таблице. Пример: 'Main'

LICENSE_KEY = ""

SOFTWARE_MODE = 0               # 0 - последовательный запуск / 1 - параллельный запуск
ACCOUNTS_IN_STREAM = 10         # Количество аккаунтов в потоке при SOFTWARE_MODE = 1
WALLETS_TO_WORK = 0             # 0 / 3 / [3, 20] / [[3, 20]]
WALLETS_TO_EXCLUDE = 0          # 0 / 3 / [3, 20] / [[3, 20]]
SHUFFLE_WALLETS = false         # Перемешивает кошельки перед запуском
SHUFFLE_ROUTE = false           # Перемешивает маршрут перед запуском

INFINITY_MODE = true            # Запускает в работу аккаунты по новому кругу. Позволит зациклить весь процесс работы
BREAK_ROUTE = false             # Прекращает выполнение маршрута, если произойдет ошибка
SAVE_PROGRESS = true            # Включает сохранение прогресса аккаунта для Classic-routes
TELEGRAM_NOTIFICATIONS = false  # Включает уведомления в Telegram

#-----------------------------------------------CHECKER CONTROL---------------------------------------------------------

NEEDED_WALLETS_CHECKER = 0      # 0 / 3 / [3, 20] / [[3, 20]]. Аналогично работе WALLETS_TO_WORK

#------------------------------------------------SLEEP CONTROL----------------------------------------------------------

SLEEP_MODE = true                        # Включает сон после каждого модуля и аккаунта
SLEEP_TIME_QUESTS = [30, 60]             # (минимум, максимум) секунд | Время сна между квестами
SLEEP_TIME_CAMPAIGNS = [30, 60]          # (минимум, максимум) секунд | Время сна между кампаниями
SLEEP_TIME_MODULES = [20, 30]            # (минимум, максимум) секунд | Время сна между модулями
SLEEP_TIME_ACCOUNTS = [20, 30]           # (минимум, максимум) секунд | Время сна между аккаунтами
SLEEP_TIME_NEW_RUN = [28800, 29000]  # (минимум, максимум) секунд | Время сна между повторными запусками.

#------------------------------------------------RETRY CONTROL---------------------------------------------------------

MAXIMUM_RETRY = 20               # Количество повторений при ошибках
SLEEP_TIME_RETRY = [100, 120]      # (минимум, максимум) секунд | Время сна после очередного повторения

#-----------------------------------------------------GAS CONTROL-------------------------------------------------------

GAS_CONTROL = true              # Включает контроль газа
MAXIMUM_GWEI = 30               # Максимальный GWEI для работы софта, изменять во время работы софта в maximum_gwei.json
SLEEP_TIME_GAS = 100            # Время очередной проверки газа
CONTROL_TIMES_FOR_SLEEP = 5     # Количество проверок
GAS_LIMIT_MULTIPLIER = 1.5      # Множитель газ лимита для транзакций. Поможет сэкономить на транзакциях
GAS_PRICE_MULTIPLIER = 1.2      # Множитель цены газа для транзакций. Ускоряет выполнение или уменьшает цену транзакции

#------------------------------------------------PROXY CONTROL----------------------------------------------------------
PROXY_REPLACEMENT_COUNT = 20    # Максимально возможное количество раз для замены прокси
PROXY_POSSIBLE_PORTS = [10000, 19999] # Возможные порты для генерации новых прокси
PROXY_STATIC_SESSIONS = false    # Ставьте true, если используете статичные резидентские прокси

SOCKS5_ENABLE = false           # Включает использование Socks5 проксей, где это возможно. ВНИМАНИЕ! можно использовать, если порты на HTTP и SOCKS5 одинаковые

MAIN_PROXY = ''                 # Если указать, то софт будет получать цены токенов через этот прокси формат: socks5://log:pass@ip:port
USE_PROXY = true                # Включает использование прокси
MOBILE_PROXY = false            # Укажите true, если используете мобильные прокси на всех аккаунтах
SSL_CLIENT = false              # включение SSL сертификата для клиента (99% запросов)
SSL_IMAP = true                 # включение SSL сертификата для работы с почтами

#------------------------------------------------SECURE DATA------------------------------------------------------------

STATIC_W_TOKEN = false          # Использовать один и тот же принцип нахождения W токена для решетеля капчи на Galxe (экономит трафик прокси, но в теории может быть палевно)
USE_FREE_GALXE_SOLVER = true    # Использовать решение капчи через данный софт, что является бесплатным
CAPTCHA_SOLVER = 'capmonster'   # Сервис, через который будет решаться капча (2captcha или capmonster)
# https://2captcha.com/enterpage
TWO_CAPTCHA_API_KEY = ""
# https://capmonster.cloud
CAP_MONSTER_API_KEY = ""

# EXCEL AND GOOGLE INFO
EXCEL_PASSWORD = false
EXCEL_PAGE_NAME = "Main"
EXCEL_FILE_PATH = "./data/accounts_data.xlsx"  # Можете не изменять, если устраивает дефолтное расположение таблицы

# TELEGRAM DATA
TG_TOKEN = ""  # https://t.me/BotFather
TG_ID = ""  # https://t.me/getmyid_bot

# OKX API KEYS https://www.okx.com/ru/account/my-api
OKX_API_KEY = ""
OKX_API_SECRET = ""
OKX_API_PASSPHRAS = ""
OKX_EU_TYPE = false

# BITGET API KEYS https://www.bitget.com/ru/account/newapi
BITGET_API_KEY = ""
BITGET_API_SECRET = ""
BITGET_API_PASSPHRAS = ""
