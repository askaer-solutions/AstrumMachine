#--------------------------------------------------------AUTH-----------------------------------------------------------

# Тип авторизации:
# "email" - через email + OTP (CDP API). Требует email в accounts_data
# "private_key" - через приватный ключ (EIP-7702). Есть 2 варианта:
#
#   Вариант 1: Мнемоника в EVM
#     - Вставить 12 слов мнемоники в колонку EVM
#     - Solana оставить ПУСТЫМ
#     - Все 10 адресов HD-кошелька деривируются автоматически из мнемоники
#
#   Вариант 2: Приватный ключ в EVM
#     - Вставить приватный ключ в колонку EVM
#     - В Solana вставить 9 фейковых приватных ключей через запятую (без 0x)
#     - Пример: abc123def456...,def789ghi012...,jkl345mno678...,...
#     - Нужно для имитации 10 адресов HD-кошелька (1 реальный + 9 фейковых)
#

BASEAPP_FLOW_TYPE = "email"

#---------------------------------------------------------CEX-----------------------------------------------------------

# Сумма ETH для вывода с CEX на SCW (мин, макс)
# Можно указать проценты от баланса CEX: ["10", "20"] = 10-20%
BASEAPP_WITHDRAW_ETH_AMOUNT = [0.001, 0.004]

#---------------------------------------------------MIN BALANCE---------------------------------------------------------

# Минимальный баланс ETH на кошельке в USD
# Если баланс ниже этого значения — активности с затратами (свапы, создание коинов) будут пропущены
# Чтобы не обнулять кошелёк и не пополнять его заново
# 0 = проверка отключена
BASEAPP_MIN_BALANCE_USD = 0.5

#------------------------------------------------------PROFILE----------------------------------------------------------

[BASEAPP_PROFILE]
# Режим получения basename:
# "table" - из таблицы accounts_data (колонка Nickname), прямое соответствие по номеру строки
# "file" - из файла data/services/baseapp/basenames.txt, случайный выбор из списка
# "auto" - автоматическая генерация
basename_mode = "auto"


#--------------------------------------------------------SWAP-----------------------------------------------------------

[BASEAPP_SWAP]
# Список контрактов токенов для покупки (ETH -> token)
# Каждый запуск выбирается случайный токен из списка
token_contracts = [
    "0x00cBeA249e...",  # Token 1
    "0xDF6Fa7147e...",  # Token 2
]

# Сумма ETH для свапа (мин, макс)
# Можно указать проценты от баланса: ["10", "20"] = 10-20%
amount = ["1", "2"]

#--------------------------------------------------AVAILABLE MODULES----------------------------------------------------

#  baseapp_register             # Регистрация аккаунта: CDP кошелёк, Smart Wallet и basename
#  baseapp_swap_eth_to_token    # Свап ETH в случайный токен из BASEAPP_SWAP.token_contracts
#  baseapp_swap_token_to_eth    # Свап всех токенов из BASEAPP_SWAP.token_contracts обратно в ETH
#  baseapp_swap_coins_to_eth    # Свап всех Zora коинов обратно в ETH (для своих резерв 10M токенов)
#  baseapp_cex_withdraw_eth     # Вывод ETH с CEX на SCW кошелек. Сумма по BASEAPP_WITHDRAW_ETH_AMOUNT
#  baseapp_verify_x             # Привязка X (Twitter) к профилю Base через verify.base.dev (подпись SCW + OAuth2)

# ВНИМАНИЕ: в феврале 2026 Base App убрал соц-слой на Farcaster — посты, лайки,
# подписки и смена имени/био/аватара больше не работают на стороне сервера.
# Соответствующие модули (daily_activity, create_coin_post, change_*) удалены.
