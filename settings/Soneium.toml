SONEIUM_WANTED_TX_COUNT = [45, 50]      # необходимое количество транзакций
SONEIUM_WANT_BALANCE = [0.005, 0.006]   # сумма MON на балансе в Monad, при не хватке, софт запустит процесс вывода + рефьюла в сеть

SONEIUM_MIN_USDC_COLLETCOR = 0.1        # минимальное кол-во USDC для сбора в ETH
SONEIUM_MIN_WETH_COLLETCOR = 0.0001     # минимальное кол-во WETH для сбора в ETH
SONEIUM_MIN_SAKE_COLLETCOR = 0.0001     # минимальное кол-во LP токенов для сбора в ETH
SONEIUM_MIN_IONIC_COLLETCOR = 0.0001    # минимальное кол-во LP токенов для сбора в ETH

SONEIUM_CREDENTIAL_TO_SKIP = [
    514433168942563328,  # https://app.galxe.com/quest/credential/514433168942563328
    513985145116557312,  # https://app.galxe.com/quest/credential/514433168942563328
]

# Нужно ли выполнять задание минтом Somnia - RubyScore ID (0.0015 ETH в Base)
SONEIUM_RUBYSCORE_ENABLED = true

# Попробовать автоматически докинуть газ через smart refuel, если его не хватает в Base (true — да, false — просто выдать ошибку)
# Если true, то обязательно настрой:
# - BRIDGE_L2_NETWORKS — список сетей, откуда брать газ (например: ["Arbitrum", "Optimism"])
# - BRIDGE_AMOUNT — примерный объём газа, который нужно перекинуть ~ 0.0015 ETH
# - REFUEL_DAPP — 1 (GasZip. MeMeBridge не поддерживается для этого роута)
# - CEX_USING — ID биржи, если будет задействован вывод, при недостаточном балансе

SONEIUM_RUBYSCORE_SMART_REFUEL = true

# Сумма свапов на всех дапках (применяется к ETH) (можно в % или в количестве)
SONEIUM_SWAP_AMOUNT = ['55', '60']

# Сумма депозита на AAVE (применяется к ETH) (можно в % или в количестве)
SONEIUM_LANDING_AMOUNT = ["10", "20"]

# сумма для врапа, применяется для ETH, обратно 100%
SONEIUM_ETH_WRAP_AMOUNT = ['80', '90']

# проходить ли optIn при запуске бонусных кампаний на портале
SONEIUM_PROCESS_OPTIN = true

# Galxe кампании Startale (ID кампании из URL: https://app.galxe.com/quest/Startale/{ID})
SONEIUM_GALXE_CAMPAIGNS = ['GCpq5tYXus']

# Sake sSuperUSD lending
# Сумма свапа ETH → USDC.e через Relay (применяется к ETH балансу)
SONEIUM_SAKE_USDC_AMOUNT = [0.0112, 0.0113]

# Сумма для Borrow USDC.e из Sake (в USDC.e)
SONEIUM_SAKE_BORROW_AMOUNT = [10, 11]

# Kyo USDT₀/ETH LP
# Сумма ETH для депозита в USDT₀/ETH пул (применяется к ETH балансу)
SONEIUM_KYO_LP_AMOUNT = [0.002, 0.003]

# ─────────────────── Bonus Score Quests (Apr-May 2026) ───────────────────────
# Кампании на portal.startale.com / Bonus Score (4 dApps × +7.5 баллов).
#
# Quest 1 — Startale: Complete swap for ≥ 1 USDSC (через UniversalRouter)
# Quest 2 — SoundChains: Generate and mint your music (NFT mint)
# Quest 3 — Superstake: Play 10 rounds of the Claw Machine (3 free игры/день)
# Quest 4 — ELHEXA - Isekai Infinite: Complete the special daily check-in (3 дня)

# Сумма ETH для свапа в USDSC через Startale UniversalRouter (только цифры, не проценты).
# Минимум по квесту: 1 USDSC. При курсе ~2275 USDSC/ETH:
#   0.0006 ETH → ~1.36 USDSC, 0.0008 ETH → ~1.82 USDSC — с запасом на slippage и колебания курса.
# Если ETH сильно вырастет — можно опустить значения; если упадёт — поднять.
SONEIUM_STARTALE_SWAP_AMOUNT = [0.0006, 0.0008]

# Сколько раундов Claw Machine играть за один запуск Superstake.
# Бесплатных игр у CashOrCrash 3 в сутки — больше указывать смысла нет, но если
# выставить меньше, можно растянуть выполнение квеста на больше дней.
SONEIUM_SUPERSTAKE_GAMES_PER_RUN = 3

# Quest — Burrow Bash (Whack-a-Mole demo): Mini Apps Carnival "Play Burrow Bash"
# и Bonus Score "Play 10 games" заполняются играми в демо-режиме (бесплатно, без
# реальной ставки ETH).
#
# Сколько игр запускать за один прогон модуля.
#   - 1 хватит для Mini Apps Carnival (нужна 1 игра)
#   - 10 нужно для Bonus Score "Play 10 games" (накапливается между прогонами)
# Лимит сервера: 10 hammers/день — больше 10 игр не сыграть всё равно.
BURROW_BASH_GAMES_COUNT = 10

# Сколько безопасных пиков делать в каждой игре перед cashout.
# Чем больше пиков — тем выше шанс наступить на крота и потерять multiplier
# (но при бесплатном демо это всё равно не влияет на квест — игра засчитывается
# в любом случае при PUT complete-mission).
BURROW_BASH_PICKS_PER_GAME = 2
