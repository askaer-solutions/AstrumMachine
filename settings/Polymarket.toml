#----------------------------------------------------POLYMARKET VOLUME-----------------------------------------------------

# Кол-во циклов покупки продажи через лимитные ордера
POLYMARKET_CIRCLE_COUNT = [5, 10]

# Интервал проверки состояния открытого ордера (актуальная цена в стакане/заполнение)
POLYMARKET_MONITOR_SLEEP = 10

# Надбавка к цене продажи в центах. Пример: Покупка по 0.50$, MIN_PROFIT_CENTS = 5 → продажа по 0.55$
POLYMARKET_MIN_PROFIT_CENTS = 0.1

# Максимальная цена позиции, выше которой софт не будет открывать ордера
POLYMARKET_MAX_BUY_PRICE = 0.998

# Стоп-лосс в центах. Если цена упадет на эту величину от цены покупки - маркет продажа. 0 = отключен
POLYMARKET_STOP_LOSS_CENTS = 20

# Минимальный процент заполнения BUY ордера для считывания выполненным (50 = 50%)
POLYMARKET_BUY_ORDER_FILL_PERCENTAGE = 50

# Порог очереди для реордера. 0.5 = реордер если очередь > 50% от нашего размера
POLYMARKET_REORDER_SIZE_THRESHOLD = 0.5

# Диапазон сумм для размещения ордеров (USDC)
POLYMARKET_ORDER_AMOUNT = [10, 10]

# Таймаут на покупку (секунды)
POLYMARKET_BUY_TIMEOUT = 2600000 # 1месяц+

# Таймаут на продажу (секунды)
POLYMARKET_SELL_TIMEOUT = 2600000 # 1месяц+

# Таймаут ожидания подтверждения трейдов на чейне (секунды)
POLYMARKET_TRADE_CONFIRM_TIMEOUT = 600

# Таймаут ожидания Safe транзакции (секунды)
POLYMARKET_TX_WAIT_TIMEOUT = 600

# Таймаут бриджа для deposit/withdraw (секунды)
POLYMARKET_BRIDGE_TIMEOUT = 1200

# Ссылки на маркеты для крутки волюма
# Формат: Список списков [URL, ИСХОД]
# Также можно указать в столбце SUI таблицы accounts_data.xlsx
# Формат для SUI столбца: url1,YES,url2,NO
POLYMARKET_ORDERS_URL = [
    ["https://polymarket.com/event/what-price-will-bitcoin-hit-in-february-2026/will-bitcoin-reach-120k-in-february-2026", "NO"],
    ["https://polymarket.com/event/what-price-will-ethereum-hit-in-february-2026/will-ethereum-reach-3800-in-february-2026", "NO"],
]

#---------------------------------------------------POLYMARKET WITHDRAW----------------------------------------------------

POLYMARKET_WITHDRAW_CHAIN = "Base"
POLYMARKET_WITHDRAW_TOKEN = "ETH"
POLYMARKET_WITHDRAW_AMOUNT = ["100", "100"]
POLYMARKET_WITHDRAW_KEEP_AMOUNT = 1.0

#----------------------------------------------------POLYMARKET DEPOSIT----------------------------------------------------

POLYMARKET_DEPOSIT_CHAINS = ["Base", "Arbitrum", "Optimism"]
POLYMARKET_DEPOSIT_TOKEN = "ETH"
POLYMARKET_DEPOSIT_AMOUNT = ["100", "100"]


#===================================================================================================================
#                                    PREDICTION FORKS (Арбитраж Opinion ↔ Polymarket)
#===================================================================================================================

# Ссылки на события для арбитража
# ВАЖНО: События на Opinion и Polymarket должны быть идентичными (один и тот же исход)
FORKS_OPINION_URL = [
    "https://app.opinion.trade/detail?topicId=5565"
]

FORKS_POLYMARKET_URL = [
    "https://polymarket.com/event/bitcoin-up-or-down-on-february-4"
]

#-------------------------------------------------------------------------------------------------------------------
#                                              ЦЕНОВЫЕ ФИЛЬТРЫ
#-------------------------------------------------------------------------------------------------------------------

# Минимальная цена позиции (в центах)
# Позиции с ценой ниже этого значения игнорируются (слишком рискованные)
# Рекомендуется: 15-25¢ для консервативной торговли, 5-10¢ для агрессивной
FORKS_MIN_PRICE_CENTS = 15

# Минимальная цена продажи на Opinion (в центах)
# Продажа происходит только если цена на Opinion > этого значения
# Закупка через SPLIT всегда по 50¢, поэтому продажа должна быть выше для профита
# Рекомендуется: 51-55¢ для гарантии профита после комиссий
FORKS_MIN_SELL_PRICE_CENTS = 51

# Максимальная цена позиции (в центах)
# Позиции с ценой выше этого значения игнорируются (малый потенциал роста)
# Рекомендуется: 80-85¢ для консервативной торговли, 90-95¢ для агрессивной
FORKS_MAX_PRICE_CENTS = 99

# Минимальный спред между платформами (в центах)
# Сделка открывается только если разница цен >= этого значения
# Рекомендуется: 2-3¢ для покрытия комиссий и гарантии профита
FORKS_MIN_SPREAD_CENTS = 0

#-------------------------------------------------------------------------------------------------------------------
#                                            ПАРАМЕТРЫ СПЛИТА
#-------------------------------------------------------------------------------------------------------------------

# Сумма для SPLIT операции на Opinion (в USDT)
# При SPLIT вы получаете равное количество YES и NO токенов
# Формат: ["минимум", "максимум"] — случайное значение из диапазона
# Рекомендуется: начинать с 50-100$ для тестов, 200-500$ для продакшена
FORKS_SPLIT_AMOUNT = [50, 50]

#-------------------------------------------------------------------------------------------------------------------
#                                         ЗАЩИТА ОТ БОТОВ (Anti-Bot)
#-------------------------------------------------------------------------------------------------------------------

# Количество последовательных снижений цены для детекции бота
# Если конкурент перебивает вашу цену N раз подряд за короткий период — это бот
# Рекомендуется: 4-6 для нормальных рынков, 8-10 для высоколиквидных
FORKS_BOT_DETECTION_DROPS = 4

# Время ожидания после детекции бота (в секундах)
# После обнаружения бота софт ставит торговлю на паузу
# Рекомендуется: 1800-3600 (30-60 минут) чтобы бот ушёл с рынка
FORKS_BOT_WAIT_TIME = 1800

#-------------------------------------------------------------------------------------------------------------------
#                                          ПАРАМЕТРЫ ЭКСПИРАЦИИ
#-------------------------------------------------------------------------------------------------------------------

# Кастомное время окончания события (перезаписывает время из API)
# Формат: "YYYY-MM-DD HH:MM" в ET (Eastern Time) timezone
# Пример: "2026-02-05 12:00" = 5 февраля 2026 в 12:00 ET
# Оставьте пустым "" чтобы использовать время из API
FORKS_EVENT_END_TIME = ""

# Альтернатива: длительность события в часах от текущего момента
# Используется только если FORKS_EVENT_END_TIME пустой И API не вернул время
# 0 = отключено (использовать только API или FORKS_EVENT_END_TIME)
# Пример: 24 = событие закончится через 24 часа от запуска
FORKS_EVENT_DURATION_HOURS = 0

# Предупреждение о близости экспирации (в секундах до конца события)
# За это время до окончания события софт начинает процедуру закрытия:
# - Отменяет открытые ордера
# - Делает MERGE оставшихся токенов
# - Продаёт остатки по маркету
# Рекомендуется: 1800 (30 минут) для безопасного закрытия

FORKS_EVENT_CLOSE_WARNING = 1800

# Время ожидания после отмены ордера из-за низкого спреда (в секундах)
# Если спред упал ниже минимума, ордер отменяется и софт ждёт это время
# перед повторной попыткой выставить ордер
# Рекомендуется: 60 секунд для быстрой реакции на восстановление спреда
FORKS_LOW_SPREAD_WAIT_TIME = 60

#-------------------------------------------------------------------------------------------------------------------
#                                    ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ (Advanced)
#-------------------------------------------------------------------------------------------------------------------

# Порог для ребаланса позиции
# Используется для определения когда делать дополнительный ребаланс
# 0.5 = ребаланс если дисбаланс > 50% от размера позиции
FORKS_REBALANCE_THRESHOLD = 0.5

#-------------------------------------------------------------------------------------------------------------------
#                                               MARKET МОДУЛЬ
#-------------------------------------------------------------------------------------------------------------------

# Сумма для маркет-покупки (в USDT)
# Формат: ["минимум", "максимум"] — случайное значение из диапазона
FORKS_MARKET_BUY_AMOUNT = ["10", "20"]

# Порог суммы для выбора дельты (в USD)
FORKS_MARKET_BUY_THRESHOLD = 20

# Минимальная дельта для FORKS_MARKET_BUY_AMOUNT меньше FORKS_MARKET_BUY_THRESHOLD (в центах)
FORKS_MARKET_BUY_MIN_DELTA_SMALL = 0.1

# Минимальная дельта для FORKS_MARKET_BUY_AMOUNT больше FORKS_MARKET_BUY_THRESHOLD (в центах)
FORKS_MARKET_BUY_MIN_DELTA_LARGE = 5.0

# Интервал проверки спреда (в секундах)
FORKS_MARKET_BUY_CHECK_INTERVAL = 5

# Максимальное количество покупок (0 = без лимита)
FORKS_MARKET_BUY_MAX_EXECUTIONS = 0

# Максимальная цена для маркет-покупки (в центах)
FORKS_MARKET_BUY_MAX_PRICE_CENTS = 100
